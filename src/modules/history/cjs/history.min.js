function _interopDefault(t) {
    return t && typeof t === 'object' && 'default' in t ? t.default : t;
}
Object.defineProperty(exports, '__esModule', {value: !0});
const resolvePathname = _interopDefault(require('resolve-pathname'));
const valueEqual = _interopDefault(require('value-equal'));
require('tiny-warning');
const invariant = _interopDefault(require('tiny-invariant'));

function _extends() {
    return (_extends =
        Object.assign ||
        function(t) {
            for (let n = 1; n < arguments.length; n++) {
                const e = arguments[n];
                for (const a in e) {
                    Object.prototype.hasOwnProperty.call(e, a) && (t[a] = e[a]);
                }
            }
            return t;
        }).apply(this, arguments);
}
function addLeadingSlash(t) {
    return t.charAt(0) === '/' ? t : `/${t}`;
}
function stripLeadingSlash(t) {
    return t.charAt(0) === '/' ? t.substr(1) : t;
}
function hasBasename(t, n) {
    return (
        t.toLowerCase().indexOf(n.toLowerCase()) === 0 && '/?#'.indexOf(t.charAt(n.length)) !== -1
    );
}
function stripBasename(t, n) {
    return hasBasename(t, n) ? t.substr(n.length) : t;
}
function stripTrailingSlash(t) {
    return t.charAt(t.length - 1) === '/' ? t.slice(0, -1) : t;
}
function parsePath(t) {
    let n = t || '/';
    let e = '';
    let a = '';
    const r = n.indexOf('#');
    r !== -1 && ((a = n.substr(r)), (n = n.substr(0, r)));
    const o = n.indexOf('?');
    return (
        o !== -1 && ((e = n.substr(o)), (n = n.substr(0, o))),
        {pathname: n, search: e === '?' ? '' : e, hash: a === '#' ? '' : a}
    );
}
function createPath(t) {
    const n = t.pathname;
    const e = t.search;
    const a = t.hash;
    let r = n || '/';
    return (
        e && e !== '?' && (r += e.charAt(0) === '?' ? e : `?${e}`),
        a && a !== '#' && (r += a.charAt(0) === '#' ? a : `#${a}`),
        r
    );
}
function createLocation(t, n, e, a) {
    let r;
    typeof t === 'string'
        ? ((r = parsePath(t)).state = n)
        : (void 0 === (r = {...t}).pathname && (r.pathname = ''),
          r.search ? r.search.charAt(0) !== '?' && (r.search = `?${r.search}`) : (r.search = ''),
          r.hash ? r.hash.charAt(0) !== '#' && (r.hash = `#${r.hash}`) : (r.hash = ''),
          void 0 !== n && void 0 === r.state && (r.state = n));
    try {
        r.pathname = decodeURI(r.pathname);
    } catch (t) {
        throw t instanceof URIError
            ? new URIError(
                  `Pathname "${r.pathname}" could not be decoded. This is likely caused by an invalid percent-encoding.`
              )
            : t;
    }
    return (
        e && (r.key = e),
        a
            ? r.pathname
                ? r.pathname.charAt(0) !== '/' &&
                  (r.pathname = resolvePathname(r.pathname, a.pathname))
                : (r.pathname = a.pathname)
            : r.pathname || (r.pathname = '/'),
        r
    );
}
function locationsAreEqual(t, n) {
    return (
        t.pathname === n.pathname &&
        t.search === n.search &&
        t.hash === n.hash &&
        t.key === n.key &&
        valueEqual(t.state, n.state)
    );
}
function createTransitionManager() {
    let o = null;
    let a = [];
    return {
        setPrompt(t) {
            return (
                (o = t),
                function() {
                    o === t && (o = null);
                }
            );
        },
        confirmTransitionTo(t, n, e, a) {
            if (o != null) {
                const r = typeof o === 'function' ? o(t, n) : o;
                typeof r === 'string' ? (typeof e === 'function' ? e(r, a) : a(!0)) : a(!1 !== r);
            } else {
                a(!0);
            }
        },
        appendListener(t) {
            let n = !0;
            function e() {
                n && t.apply(void 0, arguments);
            }
            return (
                a.push(e),
                function() {
                    (n = !1),
                        (a = a.filter(function(t) {
                            return t !== e;
                        }));
                }
            );
        },
        notifyListeners() {
            for (var t = arguments.length, n = new Array(t), e = 0; e < t; e++) {
                n[e] = arguments[e];
            }
            a.forEach(function(t) {
                return t.apply(void 0, n);
            });
        }
    };
}
const canUseDOM = !(
    typeof window === 'undefined' ||
    !window.document ||
    !window.document.createElement
);
function getConfirmation(t, n) {
    n(window.confirm(t));
}
function supportsHistory() {
    const t = window.navigator.userAgent;
    return (
        ((t.indexOf('Android 2.') === -1 && t.indexOf('Android 4.0') === -1) ||
            t.indexOf('Mobile Safari') === -1 ||
            t.indexOf('Chrome') !== -1 ||
            t.indexOf('Windows Phone') !== -1) &&
        window.history &&
        'pushState' in window.history
    );
}
function supportsPopStateOnHashChange() {
    return window.navigator.userAgent.indexOf('Trident') === -1;
}
function supportsGoWithoutReloadUsingHash() {
    return window.navigator.userAgent.indexOf('Firefox') === -1;
}
function isExtraneousPopstateEvent(t) {
    return void 0 === t.state && navigator.userAgent.indexOf('CriOS') === -1;
}
const PopStateEvent = 'popstate';
const HashChangeEvent = 'hashchange';
function getHistoryState() {
    try {
        return window.history.state || {};
    } catch (t) {
        return {};
    }
}
function createBrowserHistory(t) {
    void 0 === t && (t = {}), canUseDOM || invariant(!1);
    const s = window.history;
    const c = supportsHistory();
    const n = !supportsPopStateOnHashChange();
    const e = t;
    const a = e.forceRefresh;
    const h = void 0 !== a && a;
    const r = e.getUserConfirmation;
    const u = void 0 === r ? getConfirmation : r;
    const o = e.keyLength;
    const i = void 0 === o ? 6 : o;
    const f = t.basename ? stripTrailingSlash(addLeadingSlash(t.basename)) : '';
    function l(t) {
        const n = t || {};
        const e = n.key;
        const a = n.state;
        const r = window.location;
        let o = r.pathname + r.search + r.hash;
        return f && (o = stripBasename(o, f)), createLocation(o, a, e);
    }
    function d() {
        return Math.random()
            .toString(36)
            .substr(2, i);
    }
    const v = createTransitionManager();
    function p(t) {
        _extends(T, t), (T.length = s.length), v.notifyListeners(T.location, T.action);
    }
    function g(t) {
        isExtraneousPopstateEvent(t) || w(l(t.state));
    }
    function P() {
        w(l(getHistoryState()));
    }
    let m = !1;
    function w(n) {
        if (m) {
            (m = !1), p();
        } else {
            v.confirmTransitionTo(n, 'POP', u, function(t) {
                t
                    ? p({action: 'POP', location: n})
                    : (function(t) {
                          const n = T.location;
                          let e = H.indexOf(n.key);
                          e === -1 && (e = 0);
                          let a = H.indexOf(t.key);
                          a === -1 && (a = 0);
                          const r = e - a;
                          r && ((m = !0), L(r));
                      })(n);
            });
        }
    }
    const y = l(getHistoryState());
    var H = [y.key];
    function x(t) {
        return f + createPath(t);
    }
    function L(t) {
        s.go(t);
    }
    let O = 0;
    function E(t) {
        (O += t) === 1 && t === 1
            ? (window.addEventListener(PopStateEvent, g),
              n && window.addEventListener(HashChangeEvent, P))
            : O === 0 &&
              (window.removeEventListener(PopStateEvent, g),
              n && window.removeEventListener(HashChangeEvent, P));
    }
    let S = !1;
    var T = {
        length: s.length,
        action: 'POP',
        location: y,
        createHref: x,
        push(t, n) {
            const i = createLocation(t, n, d(), T.location);
            v.confirmTransitionTo(i, 'PUSH', u, function(t) {
                if (t) {
                    const n = x(i);
                    const e = i.key;
                    const a = i.state;
                    if (c) {
                        if ((s.pushState({key: e, state: a}, null, n), h)) {
                            window.location.href = n;
                        } else {
                            const r = H.indexOf(T.location.key);
                            const o = H.slice(0, r + 1);
                            o.push(i.key), (H = o), p({action: 'PUSH', location: i});
                        }
                    } else {
                        window.location.href = n;
                    }
                }
            });
        },
        replace(t, n) {
            const o = 'REPLACE';
            const i = createLocation(t, n, d(), T.location);
            v.confirmTransitionTo(i, o, u, function(t) {
                if (t) {
                    const n = x(i);
                    const e = i.key;
                    const a = i.state;
                    if (c) {
                        if ((s.replaceState({key: e, state: a}, null, n), h)) {
                            window.location.replace(n);
                        } else {
                            const r = H.indexOf(T.location.key);
                            r !== -1 && (H[r] = i.key), p({action: o, location: i});
                        }
                    } else {
                        window.location.replace(n);
                    }
                }
            });
        },
        go: L,
        goBack() {
            L(-1);
        },
        goForward() {
            L(1);
        },
        block(t) {
            void 0 === t && (t = !1);
            const n = v.setPrompt(t);
            return (
                S || (E(1), (S = !0)),
                function() {
                    return S && ((S = !1), E(-1)), n();
                }
            );
        },
        listen(t) {
            const n = v.appendListener(t);
            return (
                E(1),
                function() {
                    E(-1), n();
                }
            );
        }
    };
    return T;
}
const HashChangeEvent$1 = 'hashchange';
const HashPathCoders = {
    hashbang: {
        encodePath(t) {
            return t.charAt(0) === '!' ? t : `!/${stripLeadingSlash(t)}`;
        },
        decodePath(t) {
            return t.charAt(0) === '!' ? t.substr(1) : t;
        }
    },
    noslash: {encodePath: stripLeadingSlash, decodePath: addLeadingSlash},
    slash: {encodePath: addLeadingSlash, decodePath: addLeadingSlash}
};
function stripHash(t) {
    const n = t.indexOf('#');
    return n === -1 ? t : t.slice(0, n);
}
function getHashPath() {
    const t = window.location.href;
    const n = t.indexOf('#');
    return n === -1 ? '' : t.substring(n + 1);
}
function pushHashPath(t) {
    window.location.hash = t;
}
function replaceHashPath(t) {
    window.location.replace(`${stripHash(window.location.href)}#${t}`);
}
function createHashHistory(t) {
    void 0 === t && (t = {}), canUseDOM || invariant(!1);
    const n = window.history;
    const e = (supportsGoWithoutReloadUsingHash(), t);
    const a = e.getUserConfirmation;
    const i = void 0 === a ? getConfirmation : a;
    const r = e.hashType;
    const o = void 0 === r ? 'slash' : r;
    const s = t.basename ? stripTrailingSlash(addLeadingSlash(t.basename)) : '';
    const c = HashPathCoders[o];
    const h = c.encodePath;
    const u = c.decodePath;
    function f() {
        let t = u(getHashPath());
        return s && (t = stripBasename(t, s)), createLocation(t);
    }
    const l = createTransitionManager();
    function d(t) {
        _extends(E, t), (E.length = n.length), l.notifyListeners(E.location, E.action);
    }
    let v = !1;
    let p = null;
    function g() {
        const t = getHashPath();
        const n = h(t);
        if (t !== n) {
            replaceHashPath(n);
        } else {
            const e = f();
            const a = E.location;
            if (
                !v &&
                (function(t, n) {
                    return t.pathname === n.pathname && t.search === n.search && t.hash === n.hash;
                })(a, e)
            ) {
                return;
            }
            if (p === createPath(e)) {
                return;
            }
            (p = null),
                (function(n) {
                    if (v) {
                        (v = !1), d();
                    } else {
                        l.confirmTransitionTo(n, 'POP', i, function(t) {
                            t
                                ? d({action: 'POP', location: n})
                                : (function(t) {
                                      const n = E.location;
                                      let e = y.lastIndexOf(createPath(n));
                                      e === -1 && (e = 0);
                                      let a = y.lastIndexOf(createPath(t));
                                      a === -1 && (a = 0);
                                      const r = e - a;
                                      r && ((v = !0), H(r));
                                  })(n);
                        });
                    }
                })(e);
        }
    }
    const P = getHashPath();
    const m = h(P);
    P !== m && replaceHashPath(m);
    const w = f();
    var y = [createPath(w)];
    function H(t) {
        n.go(t);
    }
    let x = 0;
    function L(t) {
        (x += t) === 1 && t === 1
            ? window.addEventListener(HashChangeEvent$1, g)
            : x === 0 && window.removeEventListener(HashChangeEvent$1, g);
    }
    let O = !1;
    var E = {
        length: n.length,
        action: 'POP',
        location: w,
        createHref(t) {
            const n = document.querySelector('base');
            let e = '';
            return (
                n && n.getAttribute('href') && (e = stripHash(window.location.href)),
                `${e}#${h(s + createPath(t))}`
            );
        },
        push(t, n) {
            const o = createLocation(t, void 0, void 0, E.location);
            l.confirmTransitionTo(o, 'PUSH', i, function(t) {
                if (t) {
                    const n = createPath(o);
                    const e = h(s + n);
                    if (getHashPath() !== e) {
                        (p = n), pushHashPath(e);
                        const a = y.lastIndexOf(createPath(E.location));
                        const r = y.slice(0, a + 1);
                        r.push(n), (y = r), d({action: 'PUSH', location: o});
                    } else {
                        d();
                    }
                }
            });
        },
        replace(t, n) {
            const r = 'REPLACE';
            const o = createLocation(t, void 0, void 0, E.location);
            l.confirmTransitionTo(o, r, i, function(t) {
                if (t) {
                    const n = createPath(o);
                    const e = h(s + n);
                    getHashPath() !== e && ((p = n), replaceHashPath(e));
                    const a = y.indexOf(createPath(E.location));
                    a !== -1 && (y[a] = n), d({action: r, location: o});
                }
            });
        },
        go: H,
        goBack() {
            H(-1);
        },
        goForward() {
            H(1);
        },
        block(t) {
            void 0 === t && (t = !1);
            const n = l.setPrompt(t);
            return (
                O || (L(1), (O = !0)),
                function() {
                    return O && ((O = !1), L(-1)), n();
                }
            );
        },
        listen(t) {
            const n = l.appendListener(t);
            return (
                L(1),
                function() {
                    L(-1), n();
                }
            );
        }
    };
    return E;
}
function clamp(t, n, e) {
    return Math.min(Math.max(t, n), e);
}
function createMemoryHistory(t) {
    void 0 === t && (t = {});
    const n = t;
    const r = n.getUserConfirmation;
    const e = n.initialEntries;
    const a = void 0 === e ? ['/'] : e;
    const o = n.initialIndex;
    const i = void 0 === o ? 0 : o;
    const s = n.keyLength;
    const c = void 0 === s ? 6 : s;
    const h = createTransitionManager();
    function u(t) {
        _extends(g, t), (g.length = g.entries.length), h.notifyListeners(g.location, g.action);
    }
    function f() {
        return Math.random()
            .toString(36)
            .substr(2, c);
    }
    const l = clamp(i, 0, a.length - 1);
    const d = a.map(function(t) {
        return createLocation(t, void 0, typeof t === 'string' ? f() : t.key || f());
    });
    const v = createPath;
    function p(t) {
        const n = clamp(g.index + t, 0, g.entries.length - 1);
        const e = g.entries[n];
        h.confirmTransitionTo(e, 'POP', r, function(t) {
            t ? u({action: 'POP', location: e, index: n}) : u();
        });
    }
    var g = {
        length: d.length,
        action: 'POP',
        location: d[l],
        index: l,
        entries: d,
        createHref: v,
        push(t, n) {
            const a = createLocation(t, n, f(), g.location);
            h.confirmTransitionTo(a, 'PUSH', r, function(t) {
                if (t) {
                    const n = g.index + 1;
                    const e = g.entries.slice(0);
                    e.length > n ? e.splice(n, e.length - n, a) : e.push(a),
                        u({action: 'PUSH', location: a, index: n, entries: e});
                }
            });
        },
        replace(t, n) {
            const e = 'REPLACE';
            const a = createLocation(t, n, f(), g.location);
            h.confirmTransitionTo(a, e, r, function(t) {
                t && ((g.entries[g.index] = a), u({action: e, location: a}));
            });
        },
        go: p,
        goBack() {
            p(-1);
        },
        goForward() {
            p(1);
        },
        canGo(t) {
            const n = g.index + t;
            return n >= 0 && n < g.entries.length;
        },
        block(t) {
            return void 0 === t && (t = !1), h.setPrompt(t);
        },
        listen(t) {
            return h.appendListener(t);
        }
    };
    return g;
}
(exports.createBrowserHistory = createBrowserHistory),
    (exports.createHashHistory = createHashHistory),
    (exports.createMemoryHistory = createMemoryHistory),
    (exports.createLocation = createLocation),
    (exports.locationsAreEqual = locationsAreEqual),
    (exports.parsePath = parsePath),
    (exports.createPath = createPath);
