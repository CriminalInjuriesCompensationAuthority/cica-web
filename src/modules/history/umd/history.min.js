!(function(n, t) {
    typeof exports === 'object' && typeof module !== 'undefined'
        ? t(exports)
        : typeof define === 'function' && define.amd
        ? define(['exports'], t)
        : t((n.History = {}));
})(this, function(n) {
    function E() {
        return (E =
            Object.assign ||
            function(n) {
                for (let t = 1; t < arguments.length; t++) {
                    const e = arguments[t];
                    for (const o in e) {
                        Object.prototype.hasOwnProperty.call(e, o) && (n[o] = e[o]);
                    }
                }
                return n;
            }).apply(this, arguments);
    }
    function d(n) {
        return n.charAt(0) === '/';
    }
    function v(n, t) {
        for (let e = t, o = e + 1, r = n.length; o < r; e += 1, o += 1) {
            n[e] = n[o];
        }
        n.pop();
    }
    function i(n) {
        return n.valueOf ? n.valueOf() : Object.prototype.valueOf.call(n);
    }
    function H(n) {
        return n.charAt(0) === '/' ? n : `/${n}`;
    }
    function t(n) {
        return n.charAt(0) === '/' ? n.substr(1) : n;
    }
    function S(n, t) {
        return (function(n, t) {
            return (
                n.toLowerCase().indexOf(t.toLowerCase()) === 0 &&
                '/?#'.indexOf(n.charAt(t.length)) !== -1
            );
        })(n, t)
            ? n.substr(t.length)
            : n;
    }
    function U(n) {
        return n.charAt(n.length - 1) === '/' ? n.slice(0, -1) : n;
    }
    function a(n) {
        let t = n || '/';
        let e = '';
        let o = '';
        const r = t.indexOf('#');
        r !== -1 && ((o = t.substr(r)), (t = t.substr(0, r)));
        const i = t.indexOf('?');
        return (
            i !== -1 && ((e = t.substr(i)), (t = t.substr(0, i))),
            {pathname: t, search: e === '?' ? '' : e, hash: o === '#' ? '' : o}
        );
    }
    function j(n) {
        const t = n.pathname;
        const e = n.search;
        const o = n.hash;
        let r = t || '/';
        return (
            e && e !== '?' && (r += e.charAt(0) === '?' ? e : `?${e}`),
            o && o !== '#' && (r += o.charAt(0) === '#' ? o : `#${o}`),
            r
        );
    }
    function C(n, t, e, o) {
        let r;
        typeof n === 'string'
            ? ((r = a(n)).state = t)
            : (void 0 === (r = {...n}).pathname && (r.pathname = ''),
              r.search
                  ? r.search.charAt(0) !== '?' && (r.search = `?${r.search}`)
                  : (r.search = ''),
              r.hash ? r.hash.charAt(0) !== '#' && (r.hash = `#${r.hash}`) : (r.hash = ''),
              void 0 !== t && void 0 === r.state && (r.state = t));
        try {
            r.pathname = decodeURI(r.pathname);
        } catch (n) {
            throw n instanceof URIError
                ? new URIError(
                      `Pathname "${r.pathname}" could not be decoded. This is likely caused by an invalid percent-encoding.`
                  )
                : n;
        }
        return (
            e && (r.key = e),
            o
                ? r.pathname
                    ? r.pathname.charAt(0) !== '/' &&
                      (r.pathname = (function(n, t) {
                          void 0 === t && (t = '');
                          let e;
                          const o = (n && n.split('/')) || [];
                          let r = (t && t.split('/')) || [];
                          const i = n && d(n);
                          const a = t && d(t);
                          const c = i || a;
                          if (
                              (n && d(n) ? (r = o) : o.length && (r.pop(), (r = r.concat(o))),
                              !r.length)
                          ) {
                              return '/';
                          }
                          if (r.length) {
                              const u = r[r.length - 1];
                              e = u === '.' || u === '..' || u === '';
                          } else {
                              e = !1;
                          }
                          for (var f = 0, s = r.length; s >= 0; s--) {
                              const h = r[s];
                              h === '.'
                                  ? v(r, s)
                                  : h === '..'
                                  ? (v(r, s), f++)
                                  : f && (v(r, s), f--);
                          }
                          if (!c) {
                              for (; f--; ) {
                                  r.unshift('..');
                              }
                          }
                          !c || r[0] === '' || (r[0] && d(r[0])) || r.unshift('');
                          let l = r.join('/');
                          return e && l.substr(-1) !== '/' && (l += '/'), l;
                      })(r.pathname, o.pathname))
                    : (r.pathname = o.pathname)
                : r.pathname || (r.pathname = '/'),
            r
        );
    }
    function I() {
        let i = null;
        let o = [];
        return {
            setPrompt(n) {
                return (
                    (i = n),
                    function() {
                        i === n && (i = null);
                    }
                );
            },
            confirmTransitionTo(n, t, e, o) {
                if (i != null) {
                    const r = typeof i === 'function' ? i(n, t) : i;
                    typeof r === 'string'
                        ? typeof e === 'function'
                            ? e(r, o)
                            : o(!0)
                        : o(!1 !== r);
                } else {
                    o(!0);
                }
            },
            appendListener(n) {
                let t = !0;
                function e() {
                    t && n.apply(void 0, arguments);
                }
                return (
                    o.push(e),
                    function() {
                        (t = !1),
                            (o = o.filter(function(n) {
                                return n !== e;
                            }));
                    }
                );
            },
            notifyListeners() {
                for (var n = arguments.length, t = new Array(n), e = 0; e < n; e++) {
                    t[e] = arguments[e];
                }
                o.forEach(function(n) {
                    return n.apply(void 0, t);
                });
            }
        };
    }
    const M = !(
        typeof window === 'undefined' ||
        !window.document ||
        !window.document.createElement
    );
    function R(n, t) {
        t(window.confirm(n));
    }
    const e = 'Invariant failed';
    function B(n) {
        if (!n) {
            throw new Error(e);
        }
    }
    const F = 'popstate';
    const q = 'hashchange';
    function _() {
        try {
            return window.history.state || {};
        } catch (n) {
            return {};
        }
    }
    const T = 'hashchange';
    const L = {
        hashbang: {
            encodePath(n) {
                return n.charAt(0) === '!' ? n : `!/${t(n)}`;
            },
            decodePath(n) {
                return n.charAt(0) === '!' ? n.substr(1) : n;
            }
        },
        noslash: {encodePath: t, decodePath: H},
        slash: {encodePath: H, decodePath: H}
    };
    function G(n) {
        const t = n.indexOf('#');
        return t === -1 ? n : n.slice(0, t);
    }
    function W() {
        const n = window.location.href;
        const t = n.indexOf('#');
        return t === -1 ? '' : n.substring(t + 1);
    }
    function z(n) {
        window.location.replace(`${G(window.location.href)}#${n}`);
    }
    function g(n, t, e) {
        return Math.min(Math.max(n, t), e);
    }
    (n.createBrowserHistory = function(n) {
        void 0 === n && (n = {}), M || B(!1);
        const c = window.history;
        const u = (function() {
            const n = window.navigator.userAgent;
            return (
                ((n.indexOf('Android 2.') === -1 && n.indexOf('Android 4.0') === -1) ||
                    n.indexOf('Mobile Safari') === -1 ||
                    n.indexOf('Chrome') !== -1 ||
                    n.indexOf('Windows Phone') !== -1) &&
                window.history &&
                'pushState' in window.history
            );
        })();
        const t = !(window.navigator.userAgent.indexOf('Trident') === -1);
        const e = n;
        const o = e.forceRefresh;
        const f = void 0 !== o && o;
        const r = e.getUserConfirmation;
        const s = void 0 === r ? R : r;
        const i = e.keyLength;
        const a = void 0 === i ? 6 : i;
        const h = n.basename ? U(H(n.basename)) : '';
        function l(n) {
            const t = n || {};
            const e = t.key;
            const o = t.state;
            const r = window.location;
            let i = r.pathname + r.search + r.hash;
            return h && (i = S(i, h)), C(i, o, e);
        }
        function d() {
            return Math.random()
                .toString(36)
                .substr(2, a);
        }
        const v = I();
        function p(n) {
            E(L, n), (L.length = c.length), v.notifyListeners(L.location, L.action);
        }
        function w(n) {
            !(function(n) {
                return void 0 === n.state && navigator.userAgent.indexOf('CriOS') === -1;
            })(n) && m(l(n.state));
        }
        function g() {
            m(l(_()));
        }
        let y = !1;
        function m(t) {
            if (y) {
                (y = !1), p();
            } else {
                v.confirmTransitionTo(t, 'POP', s, function(n) {
                    n
                        ? p({action: 'POP', location: t})
                        : (function(n) {
                              const t = L.location;
                              let e = O.indexOf(t.key);
                              e === -1 && (e = 0);
                              let o = O.indexOf(n.key);
                              o === -1 && (o = 0);
                              const r = e - o;
                              r && ((y = !0), b(r));
                          })(t);
                });
            }
        }
        const P = l(_());
        var O = [P.key];
        function x(n) {
            return h + j(n);
        }
        function b(n) {
            c.go(n);
        }
        let A = 0;
        function k(n) {
            (A += n) === 1 && n === 1
                ? (window.addEventListener(F, w), t && window.addEventListener(q, g))
                : A === 0 &&
                  (window.removeEventListener(F, w), t && window.removeEventListener(q, g));
        }
        let T = !1;
        var L = {
            length: c.length,
            action: 'POP',
            location: P,
            createHref: x,
            push(n, t) {
                const a = C(n, t, d(), L.location);
                v.confirmTransitionTo(a, 'PUSH', s, function(n) {
                    if (n) {
                        const t = x(a);
                        const e = a.key;
                        const o = a.state;
                        if (u) {
                            if ((c.pushState({key: e, state: o}, null, t), f)) {
                                window.location.href = t;
                            } else {
                                const r = O.indexOf(L.location.key);
                                const i = O.slice(0, r + 1);
                                i.push(a.key), (O = i), p({action: 'PUSH', location: a});
                            }
                        } else {
                            window.location.href = t;
                        }
                    }
                });
            },
            replace(n, t) {
                const i = 'REPLACE';
                const a = C(n, t, d(), L.location);
                v.confirmTransitionTo(a, i, s, function(n) {
                    if (n) {
                        const t = x(a);
                        const e = a.key;
                        const o = a.state;
                        if (u) {
                            if ((c.replaceState({key: e, state: o}, null, t), f)) {
                                window.location.replace(t);
                            } else {
                                const r = O.indexOf(L.location.key);
                                r !== -1 && (O[r] = a.key), p({action: i, location: a});
                            }
                        } else {
                            window.location.replace(t);
                        }
                    }
                });
            },
            go: b,
            goBack() {
                b(-1);
            },
            goForward() {
                b(1);
            },
            block(n) {
                void 0 === n && (n = !1);
                const t = v.setPrompt(n);
                return (
                    T || (k(1), (T = !0)),
                    function() {
                        return T && ((T = !1), k(-1)), t();
                    }
                );
            },
            listen(n) {
                const t = v.appendListener(n);
                return (
                    k(1),
                    function() {
                        k(-1), t();
                    }
                );
            }
        };
        return L;
    }),
        (n.createHashHistory = function(n) {
            void 0 === n && (n = {}), M || B(!1);
            const t = window.history;
            const e = (window.navigator.userAgent.indexOf('Firefox'), n);
            const o = e.getUserConfirmation;
            const a = void 0 === o ? R : o;
            const r = e.hashType;
            const i = void 0 === r ? 'slash' : r;
            const c = n.basename ? U(H(n.basename)) : '';
            const u = L[i];
            const f = u.encodePath;
            const s = u.decodePath;
            function h() {
                let n = s(W());
                return c && (n = S(n, c)), C(n);
            }
            const l = I();
            function d(n) {
                E(k, n), (k.length = t.length), l.notifyListeners(k.location, k.action);
            }
            let v = !1;
            let p = null;
            function w() {
                const n = W();
                const t = f(n);
                if (n !== t) {
                    z(t);
                } else {
                    const e = h();
                    const o = k.location;
                    if (
                        !v &&
                        (function(n, t) {
                            return (
                                n.pathname === t.pathname &&
                                n.search === t.search &&
                                n.hash === t.hash
                            );
                        })(o, e)
                    ) {
                        return;
                    }
                    if (p === j(e)) {
                        return;
                    }
                    (p = null),
                        (function(t) {
                            if (v) {
                                (v = !1), d();
                            } else {
                                l.confirmTransitionTo(t, 'POP', a, function(n) {
                                    n
                                        ? d({action: 'POP', location: t})
                                        : (function(n) {
                                              const t = k.location;
                                              let e = P.lastIndexOf(j(t));
                                              e === -1 && (e = 0);
                                              let o = P.lastIndexOf(j(n));
                                              o === -1 && (o = 0);
                                              const r = e - o;
                                              r && ((v = !0), O(r));
                                          })(t);
                                });
                            }
                        })(e);
                }
            }
            const g = W();
            const y = f(g);
            g !== y && z(y);
            const m = h();
            var P = [j(m)];
            function O(n) {
                t.go(n);
            }
            let x = 0;
            function b(n) {
                (x += n) === 1 && n === 1
                    ? window.addEventListener(T, w)
                    : x === 0 && window.removeEventListener(T, w);
            }
            let A = !1;
            var k = {
                length: t.length,
                action: 'POP',
                location: m,
                createHref(n) {
                    const t = document.querySelector('base');
                    let e = '';
                    return (
                        t && t.getAttribute('href') && (e = G(window.location.href)),
                        `${e}#${f(c + j(n))}`
                    );
                },
                push(n, t) {
                    const i = C(n, void 0, void 0, k.location);
                    l.confirmTransitionTo(i, 'PUSH', a, function(n) {
                        if (n) {
                            const t = j(i);
                            const e = f(c + t);
                            if (W() !== e) {
                                (p = t),
                                    (function(n) {
                                        window.location.hash = n;
                                    })(e);
                                const o = P.lastIndexOf(j(k.location));
                                const r = P.slice(0, o + 1);
                                r.push(t), (P = r), d({action: 'PUSH', location: i});
                            } else {
                                d();
                            }
                        }
                    });
                },
                replace(n, t) {
                    const r = 'REPLACE';
                    const i = C(n, void 0, void 0, k.location);
                    l.confirmTransitionTo(i, r, a, function(n) {
                        if (n) {
                            const t = j(i);
                            const e = f(c + t);
                            W() !== e && ((p = t), z(e));
                            const o = P.indexOf(j(k.location));
                            o !== -1 && (P[o] = t), d({action: r, location: i});
                        }
                    });
                },
                go: O,
                goBack() {
                    O(-1);
                },
                goForward() {
                    O(1);
                },
                block(n) {
                    void 0 === n && (n = !1);
                    const t = l.setPrompt(n);
                    return (
                        A || (b(1), (A = !0)),
                        function() {
                            return A && ((A = !1), b(-1)), t();
                        }
                    );
                },
                listen(n) {
                    const t = l.appendListener(n);
                    return (
                        b(1),
                        function() {
                            b(-1), t();
                        }
                    );
                }
            };
            return k;
        }),
        (n.createMemoryHistory = function(n) {
            void 0 === n && (n = {});
            const t = n;
            const r = t.getUserConfirmation;
            const e = t.initialEntries;
            const o = void 0 === e ? ['/'] : e;
            const i = t.initialIndex;
            const a = void 0 === i ? 0 : i;
            const c = t.keyLength;
            const u = void 0 === c ? 6 : c;
            const f = I();
            function s(n) {
                E(w, n), (w.length = w.entries.length), f.notifyListeners(w.location, w.action);
            }
            function h() {
                return Math.random()
                    .toString(36)
                    .substr(2, u);
            }
            const l = g(a, 0, o.length - 1);
            const d = o.map(function(n) {
                return C(n, void 0, typeof n === 'string' ? h() : n.key || h());
            });
            const v = j;
            function p(n) {
                const t = g(w.index + n, 0, w.entries.length - 1);
                const e = w.entries[t];
                f.confirmTransitionTo(e, 'POP', r, function(n) {
                    n ? s({action: 'POP', location: e, index: t}) : s();
                });
            }
            var w = {
                length: d.length,
                action: 'POP',
                location: d[l],
                index: l,
                entries: d,
                createHref: v,
                push(n, t) {
                    const o = C(n, t, h(), w.location);
                    f.confirmTransitionTo(o, 'PUSH', r, function(n) {
                        if (n) {
                            const t = w.index + 1;
                            const e = w.entries.slice(0);
                            e.length > t ? e.splice(t, e.length - t, o) : e.push(o),
                                s({action: 'PUSH', location: o, index: t, entries: e});
                        }
                    });
                },
                replace(n, t) {
                    const e = 'REPLACE';
                    const o = C(n, t, h(), w.location);
                    f.confirmTransitionTo(o, e, r, function(n) {
                        n && ((w.entries[w.index] = o), s({action: e, location: o}));
                    });
                },
                go: p,
                goBack() {
                    p(-1);
                },
                goForward() {
                    p(1);
                },
                canGo(n) {
                    const t = w.index + n;
                    return t >= 0 && t < w.entries.length;
                },
                block(n) {
                    return void 0 === n && (n = !1), f.setPrompt(n);
                },
                listen(n) {
                    return f.appendListener(n);
                }
            };
            return w;
        }),
        (n.createLocation = C),
        (n.locationsAreEqual = function(n, t) {
            return (
                n.pathname === t.pathname &&
                n.search === t.search &&
                n.hash === t.hash &&
                n.key === t.key &&
                (function e(t, o) {
                    if (t === o) {
                        return !0;
                    }
                    if (t == null || o == null) {
                        return !1;
                    }
                    if (Array.isArray(t)) {
                        return (
                            Array.isArray(o) &&
                            t.length === o.length &&
                            t.every(function(n, t) {
                                return e(n, o[t]);
                            })
                        );
                    }
                    if (typeof t !== 'object' && typeof o !== 'object') {
                        return !1;
                    }
                    const n = i(t);
                    const r = i(o);
                    return n !== t || r !== o
                        ? e(n, r)
                        : Object.keys({...t, ...o}).every(function(n) {
                              return e(t[n], o[n]);
                          });
                })(n.state, t.state)
            );
        }),
        (n.parsePath = a),
        (n.createPath = j),
        Object.defineProperty(n, '__esModule', {value: !0});
});
